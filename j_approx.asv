function j = j_approx(fun, x)

    alpha = 1e-6;

    f0 = fun(x);

    in_len = length(x);
    out_len = length(f0);

    dx = zeros(size(x));

    j = zeros([out_len, in_len]);

    for i = 1:in_len

        dx(i) = alpha;

        


    end

end